<loading wx:if="{{showLoading}}">加载中</loading>
<view class="noticedetails">
	<view class="details-xq pad24">
		<view class="biaoti">{{title}}</view>
		<view class="riqi">
			<text>{{time}}</text>
			<text>{{cat_name}}</text>
		</view>
		<view class="content">
			<rich-text class="r_text" nodes="<div style='word-break: break-all;display: inline-block;'>{{content}}</div>"></rich-text> 
		</view>
		
	</view>
</view>
